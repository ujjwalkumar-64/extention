spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/genie_project
    username: ujjwal
    password: ujjwal
    driver-class-name: org.postgresql.Driver

    jpa:
      hibernate:
        ddl-auto: update
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect

  application:
    name: genieBackend

server:
  port: 8098
